/**
 * Node-side entry for the Browser package. The actual web implementation is
 * exported through the package's `dsh.client` metadata and `./client` entry;
 * the Host-side loader intentionally has nothing else to mount here.
 */
export function apply(): void {}
